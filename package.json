{
  "name": "imili-design-studio-web",
  "version": "1.0.0",
  "private": true,
  "description": "Imili Design Studio website — static site served by nginx on Cloud Run.",
  "scripts": {
    "lint:html": "html-validate \"site/**/*.html\"",
    "test": "playwright test",
    "test:qa": "playwright test tests/qa.spec.js",
    "test:security": "playwright test tests/security.spec.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "1.56.1",
    "html-validate": "^9.5.0"
  }
}
